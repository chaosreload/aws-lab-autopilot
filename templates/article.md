# [中文标题：动词开头，突出核心发现或动作]

<!-- 
标题示例：
✅ "Amazon Managed Service for Apache Flink 2.2 实战：首个大版本升级全面解析"
✅ "Amazon Nova Multimodal Embeddings 实测：首个统一多模态 Embedding 模型"
❌ "Amazon Nova Multimodal Embeddings"（太短，没有信息量）
❌ "How to use XXX"（不用英文标题）
-->

!!! info "Lab 信息"
    - **难度**: ⭐ 入门 / ⭐⭐ 中级 / ⭐⭐⭐ 高级
    - **预估时间**: XX 分钟
    - **预估费用**: $X.XX（含清理）
    - **Region**: us-east-1
    - **最后验证**: YYYY-MM-DD

## 背景

<!-- 
3-5 句话讲清楚：
1. 之前的痛点是什么？
2. 这个功能/服务怎么解决的？
3. 为什么读者应该关注？

不要照搬 What's New 公告，用自己的话重新组织。
-->

## 前置条件

- AWS 账号（需要 XXX 权限）
- AWS CLI v2 已配置
- <!-- 其他工具依赖 -->

<!-- 
如果 IAM 权限较复杂，提供最小权限 Policy JSON：

<details>
<summary>最小 IAM Policy（点击展开）</summary>

```json
{
  "Version": "2012-10-17",
  "Statement": [...]
}
```

</details>
-->

## 核心概念

<!-- 
【强制章节】不可省略。在动手之前让读者建立全局认知。

内容要求：
1. 一张"关键参数/变化一览表"（如模型参数、版本差异、功能对比）
2. 如有多种模式/选项，列出"模式/选项用途表"
3. 如涉及升级/迁移，画出路径图（文字版即可）

示例（Nova MME）：
| 参数 | 说明 |
|------|------|
| Model ID | amazon.nova-2-multimodal-embeddings-v1:0 |
| 输出维度 | 256 / 384 / 1024 / 3072 |
| ...  | ... |

示例（Flink 2.2）：
| 类别 | 变化 | 影响 |
|------|------|------|
| Runtime | Java 17 默认 | 必须重新编译 |
| ...  | ... | ... |

原则：用最少的篇幅让读者知道"这东西有什么、能做什么、有什么限制"。
-->

## 动手实践

<!-- 
每个 Step 必须包含：
1. 完整 CLI 命令（含 --region，读者可直接复制执行）
2. 实测输出（关键部分，不是全部 stdout）
3. 一句话说明这步做了什么、结果是什么

Step 数量 = 测试矩阵中的测试项数量。
测试结果表中的每一行，都必须在某个 Step 中有对应的操作和输出。
不允许"有结论无过程"。
-->

### Step 1: 准备环境

```bash
# 完整命令
```

### Step 2: 核心功能验证

```bash
# 完整命令
```

**实测输出**：
```
# 关键输出
```

### Step 3: 对比实验 / A vs B

<!-- 
【强烈建议】设计对比实验：
- 新 vs 旧版本
- 模式 A vs 模式 B
- 有效值 vs 无效值
- 同一操作在不同条件下的表现

对比实验是文章的核心价值。"能用"不是发现，"A 比 B 快 3x"才是。
-->

```bash
# 完整命令
```

### Step 4: 边界与探索性测试

<!-- 
【强制】至少包含以下一类：

1. 无效值测试 — 文档说支持 A/B/C，那 D 会怎样？
   例：维度支持 256/384/1024/3072，那 512 呢？→ ValidationException
   
2. 边界值测试 — 最大/最小/空/超长输入
   例：空文本输入 → 被拒绝；75K 字符 → 超限

3. 未文档化参数探索 — API 有哪些参数文档没列全？
   例：embeddingPurpose 官方说 2 种，实测发现 7 种

这些测试是"官方未记录发现"的主要来源，也是文章与文档搬运的核心区别。
-->

```bash
# 完整命令
```

## 测试结果

<!-- 
【强制章节】用表格汇总所有测试项的结果。

每行必须对应上面某个 Step 的实际操作。
如果某项 SKIP，标注原因。
-->

| # | 测试场景 | 结果 | 关键数据 | 备注 |
|---|---------|------|---------|------|
| 1 | 基础功能 | ✅ 通过 | | |
| 2 | A vs B 对比 | ✅ 通过 | A: Xms, B: Yms | A 快 Z% |
| 3 | 边界测试 | ✅ 通过 | | 官方未记录 |
| 4 | 无效值测试 | ✅ 预期报错 | ValidationException | |

## 踩坑记录

<!-- 
【踩坑分类规则 — 只有以下两类才能进入文章】

✅ 进入文章：
- AWS 产品限制（经 aws-knowledge 确认）
- 官方未记录的行为（实测发现）
- 所有使用该功能的读者都可能遇到的问题

❌ 不进入文章（留在内部笔记）：
- 自己代码的 bug（KeyError、TypeError、ImportError）
- 环境配置问题（PATH、版本不对）
- 一次性的网络/权限问题

判断标准：这个坑是不是所有用这个 AWS 功能的读者都可能遇到？
-->

!!! warning "踩坑 1: [简洁描述]"
    [详细说明 + 触发条件 + 影响范围]
    
    ```
    # 触发的错误信息
    ```
    
    <!-- 标注来源：已查文档确认 / 实测发现，官方未记录 -->

!!! warning "踩坑 2: [简洁描述]"
    [详细说明]

<!-- 
【洞察提炼 — 对每个踩坑和关键数据点问三个问题】

1. 这对读者的生产系统意味着什么？
2. 如果读者不知道这个，最坏情况是什么？
3. 这个发现值得用 !!! warning 还是 !!! info？

规则：
- 会导致数据丢失/服务中断/费用异常 → !!! warning 或 !!! danger
- 有用但不紧急的信息 → !!! info 或 !!! tip
- 数据点本身有意义但需要上下文解释 → 在结论中展开
-->

## 费用明细

| 资源 | 单价 | 用量 | 费用 |
|------|------|------|------|
| XXX  | $X/hr | X hr | $X.XX |
| **合计** | | | **$X.XX** |

<!-- 纯 API 调用类：标注 "< $0.01" 或 "< $0.10" 即可 -->

## 清理资源

<!-- 
【强制章节】逐个资源列出删除命令。

清理顺序：先删依赖资源，再删基础资源。
VPC 相关：先检查 ENI 残留（describe-network-interfaces --filters group-id），再删 SG/子网。
-->

```bash
# 1. 停止/删除应用级资源
# 2. 删除存储资源（S3/DynamoDB）
# 3. 删除日志（CloudWatch Log Group）
# 4. 删除 IAM（先删 policy，再删 role）
# 5. 删除网络资源（先查 ENI，再删 SG/子网/VPC）
```

!!! danger "务必清理"
    Lab 完成后请执行清理步骤，避免产生意外费用。
    <!-- 如果某些资源有持续费用，特别标注：如 "Neptune Analytics 最低 ~$4.90/天" -->

## 结论与建议

<!-- 
【强制章节】不能只是"总结"，必须给读者可操作的建议。

包含以下至少一项：
1. 场景化推荐表 — "什么场景用什么配置"
2. 选型建议 — "A vs B，什么时候选 A"
3. 升级/迁移建议 — "谁该升级，谁不该"
4. 生产注意事项 — "上生产前必须知道的 N 件事"

示例（维度选择建议表）：
| 场景 | 推荐维度 | 理由 |
|------|---------|------|
| 原型验证 | 256 | 存储最小 |
| 生产通用 | 1024 | 精度与成本平衡 |

示例（升级建议）：
- ✅ 新应用：直接用 2.2
- ⚠️ 有状态应用：先评估 Kryo 兼容性
- ❌ 依赖 DataSet API：必须先迁移代码
-->

## 参考链接

- [官方文档](https://docs.aws.amazon.com/xxx)
- [AWS What's New](https://aws.amazon.com/about-aws/whats-new/xxx)
- [AWS Blog](https://aws.amazon.com/blogs/xxx)（如有）
